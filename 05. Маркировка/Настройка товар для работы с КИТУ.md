# Настройка товар для работы с КИТУ

Перед началом настройки обратите внимание на следующие моменты:

1. Если редактируем созданный товар с настроенным базовым товаром, то базовый товар отключаем.
2. Как работает настройка "Не проверять маркировку в этом продукте (для работы с КИТУ)"?
Если включить эту настройку, система перестает выполнять почти все проверки маркировки. Единственное, что она проверяет — это GTIN (штрихкод товара). Но проверка работает необычно: система не требует, чтобы GTIN совпадал, а наоборот — требует, чтобы он не совпадал (инверсия).

Таким образом, товар пропустится только если его GTIN отличается от указанного.

3. Особенности работы после настройки – если ранее продажа мелкотарки велась без КИТУ, в первое время будут встречаться два типа заказов:
  - Требующие сканирования одной маркировки (для КИГУ);
  - Требующие сканирования каждой единицы товара (если КИГУ не применялся).
4. Настройка в 1С – перейдите:
1С → "Настройки" → "Еще больше возможностей" → "Общее"
и включите работу с несколькими единицами измерений.
![](image)

# Настройки

1. Создаем товар упаковки (В любой подходящей под нее категории). Рекомендуем заполнить следующие поля (Остальные настройки опциональны):
  1. Название - из названия должно быть понятно, что это упаковка. Например: Вода Питьевая "Итламас" негазированная 1.5л (В упаковке 6шт);
  2. Отображение - включить;
  3. Включить маркировку - включить;
  4. Не валидировать маркировку в этом продукте (для работы с КИТУ) - включить;
  5. GTIN - прописываем GTIN штучного товара, который содержится в упаковке (Если это палета, то все равно нужно будет заполнить это поле);
  6. Цена - прописываем цену, как за упаковку.
![](image)

2. Переходим в 1С - "Продажи" - "Номенклатура" и создаем штучный товар (Обратите внимание, что учет любого товара ведется в штука. Не нужно заводить отдельные товары упаковок). В разделе "Основное" заполняем следующие поля:
  1. Наименование - указываем название штучного товара, которое будет отображаться в программе. Например: Вода Питьевая "Итламас" негазированная 1.5л;
  2. Наименование для печати - наименование для печати может отличаться от наименования;
  3. Продукция ИС - Упакованная вода (Или другая продукция ИС, если пользуетесь)
3. Переходим в раздел "Ед. изм" и нажимаем на "Создать". В открывшемся окне нужно заполнить следующие поля:
  1. Наименование - Нужно указать такое наименование, чтобы было понятно что это за упаковка. Например: Упаковка Италмас 1.5л негаз;
  2. ОКЕИ - упак;
  3. Коэффициент - указываем сколько в упаковке содержится штук товара.
![](image)

4. Переходим в раздел штрих коды и добавляем штрих код штучного товара, который содержится в КИТУ. Обратите внимание, что не нужно добавлять штрих код для КИТУ, как в случае с КИГУ.
![](image)

5. Переходим в раздел "CRM" - "Интернет-магазин" - "Обслуживание" - "Инструменты администратора" - "Соответствие объектов". Добавляем сопоставление товаров:
  1. Сопоставляем товар упаковки
    1. Указываем ключ идентификации (Оставляем значение по умолчанию);
    2. Идентификатор магазина и идентификатор базы - указываем ID упаковки из кабинета настроек
![](image)

    3. Ссылка на объект - выбираем единицу измерения (Обратите внимание, что выбираем единицу измерения, а не номенклатуру)
![](image)

![](image)

    4. Объект выгружен - проставляем признак
    5. Тип объекта - Справочник.ЕдиницыИзмерения
![](image)

6. Готово.
